struct Foo{
    a: int
}
for Foo {
    fn set_a(new_a: int){
        this.a = new_a;
    }
}
let fee = [
    Foo{
        a: 4
    }
]

let i = 0;
while i < len(fee){
    fee[i].set_a(5);
    i++;
}
println(fee[0].a)
