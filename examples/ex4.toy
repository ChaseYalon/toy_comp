fn find(a: int[], target: int, i: int): int{
    if len(a) == i {
        return 0-1;
    }
    if a[i] == target{
        return i;
    }
    return find(a, target, i + 1);
}

let arr = [1, 2, 3, 4, 5, 6, 7];
let x = find(arr, 3, 0);
println(x);